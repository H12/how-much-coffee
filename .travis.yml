language: elm
deploy:
    local-dir: dist/
    provider: pages
    skip-cleanup: true
    github-token: $GITHUB_TOKEN
    keep-history: true
    on:
        branch: master
after_success:
    - elm make src/Main.elm --output=dist/index.html
